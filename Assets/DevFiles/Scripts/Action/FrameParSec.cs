namespace clrev01.ClAction
{
    public enum FrameParSec
    {
        _600frames = 600,
        _180frames = 180,
        @default = 60,
        _12frames = 12,
        _06frames = 6,
        frameAdvance = 1,
    }
}